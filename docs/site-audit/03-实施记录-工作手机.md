# 03 · 实施记录 · 工作手机对比度修复（视觉协议第 2 轮）

> 对应诊断：`00-视觉诊断报告-工作手机.md` 的三处 🔴 红线（全部为 WCAG AA 实测不达标）。
> 本轮先修红线；中低severity 项（间距双轴、字阶双定义等）按协议待基准确认后另行实施。

## 修复 1 · 主按钮白字 3.65 → 4.81 ✅

- 方案：新增 `--accent-fill: #0A70DC` **只作实心按钮底**；`--accent` 本色保留作
  文字/链接/描边（深底上 5.52 ✓——若全局加深反而会把链接文字拖到 4.18）。
- 影响面：「天黑了」「开始今天」「找他说话」、小卖部全部购买按钮（`.btn.primary`）。
- 实测：白 on #0A70DC = **4.81** ✅ AA（候选 #0A78E8=4.32❌、#0664C8=5.73 过深）。

## 修复 2 · 进度条标签 2.2-3.1 → 4.7-5.8 ✅

- 根因有两层：①填充是 60-62% 半透明色，文字被罩在色膜下；②文字本体是 66% 灰。
- 修复：四条填充改**实色加深**（白字实测：精力 #0864C0=5.37 / 麻木 #7C55A8=5.18 /
  信任 #1E7E36=4.71 / 警惕 #C0332B=5.14）；标签由 muted 灰提为全白 `--label`。
- **层级修复**：绝对定位的填充 span 原本绘制在文字上层（半透明时罩住文字、实色时
  完全盖死）——`.bar { isolation: isolate }` + `.bar span { z-index: -1 }` 把填充
  压回背景层、文字提到最上。
- 实测对比（修复后）：白字 on 实色填充 = 4.71-5.37 ✅；on 空轨道 = 11.4 ✅。

## 修复 3 · 钱包支出语义色失效 ✅

- 根因：`.wallet-summary strong`（0-1-1）的 `color: var(--warm)` 以来源顺序与特异度
  压过 `.pos/.neg`（0-1-0）——支出恒为金色。
- 修复：新增 `.wallet-summary strong.pos/.neg`（0-2-1）——收入绿 #30D158（8.24 ✓）、
  支出红 #FF453A（4.89 ✓），余额/进度保持金色。暖色主题钱包共用此规则同步受益。

## 顺带（诊断 #6 字阶项的一半）

- `.title-screen h1` 裸 `29px` → `var(--fs-11)`（token 双定义收敛的第一步）。

## 回归

- `tsc` / `vitest 292` / `build` 全绿（纯 CSS + 一处常量注释，无逻辑改动）。
- after 截图：`docs/site-audit/screenshots/after/wp-day-1-today.png`（HUD 条+信任警惕
  条+主按钮）、`wp-day-6-钱包.png`（收入绿/支出红）、`wp-night-0-chain.png`（找他
  说话/开口要钱）。
- 遗留：诊断 #4-#9（间距轴收敛、--fs-11 双定义另一半、散落色族、夜态论证）——
  按协议待《01-设计基准-工作手机》确认后实施。


---

## 追加 · 设计基准 F1-F4 实施（2026-09，用户「都做」确认后）

| 步骤 | 内容 | 结果 |
|---|---|---|
| F1 字阶 | `--fs-11` 双定义合并（83 行 29px 单一定义，删 1749 行重定义）；`ledger-day`/`.bar`/`option-meta` 升 12px | ✅ 27 处替换之一 |
| F2 形状阴影 | 新令牌 `--r-pill`(999)/`--r-hair`(2)；`.btn` 22px/`.btn.small` 19px → pill；滚动条 3px→hair；裸值 2px×4→hair；11px→`--r-md`；`.bar` 5px→4px；新增 `--shadow-0`，7 处裸阴影归档（导航向上投影保留+注释） | ✅ |
| F3 散落色 | 页面底渐变全部令牌化（`--bg-glow/--bg/--page`）；`--danger-bright`（risk-burn 渐变端）；`--gold-ink`（`.btn.warm` 深字，冷暖共用）；`body color` 走 `--label` | ✅ |
| F4 间距（先行三族） | `briefing-row`/`incoming-card`/`contact-row`/`beat-card`：padding 10px 12px→12px 16px、gap 10→12、beat-card margin 10→12 | ✅ 截图 `after/wp-day-1-today.png` |

- 附带修复：styles.css 混合行尾归一化为 LF。
- 回归：tsc / 299 测试 / build 全绿。
- F4 长尾（面板余散的 6/10/14px：chat、shop、ending 等次要位置）按同法逐族推进即可，不在本轮。

---

## 追加 · F4 长尾收敛（2026-09，收尾轮）

按《01-设计基准》C 表刻度对剩余次要位置一次性收敛，共 **116 处替换 / 112 行**
（styles.css 工作手机区，行号 180-1827；逐行精确替换 + count==1 断言脚本执行）：

| 刻度 | 动作 | 覆盖族（举例） |
|---|---|---|
| 6px → 8px | 微间隙/ chips | btn gap、option-meta、target-info、moment-likes、incoming-actions、da-reasons 等 |
| 10px → 8 / 12px | 紧凑 8、分组 12 | log-entry→卡片 12/16；target-list、chat-panel、persona-grid、plan-card→12；wallet-summary→12 |
| 14px → 12 / 16px | 容器 16、次级 12 | target-card/ledger/persona-epilogue→16；chat-stream 16px 12px；bubble 10px 16px；newgame-screen→12 |
| 7 / 3 / 18 / 22px → 就近 | 8 / 4 / 16 / 20 | risk-strip 7→8；nav-btn gap 3→4；ending-screen 18→16；title/prologue-screen 22→20 |

- 明确不动：`.h1-ver { margin-left: 3px }`（v2.0 角标偏移，用户三轮手调值）；
  舒适圈区段（styles.css ≥1902 行，有自己的 `--c-s-*` 令牌体系且已单独验收）。
- 事故记录：截图脚本默认输出目录是 `before/`，首跑覆盖基线 8 张——已 `git checkout`
  恢复，并以显式参数重跑至 `after/`。教训：`work-shots.mjs` 的 OUT 参数是必传项。
- 回归：tsc / 299 测试 / build 全绿；after 全套 8 张截图无布局破坏。
