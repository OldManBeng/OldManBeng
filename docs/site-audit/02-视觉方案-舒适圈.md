# 02 · 视觉方案 · 舒适圈（常用手机）

> 协议：《视觉优化协议.md》第 3 阶段产出（只出方案，不改代码）。
> 每处修改标注：现状问题 → 调整内容 → 对照《01-设计基准》哪一条。
> 改动总量预估：`styles.css` 舒适圈区（1840-2366）+ `comfort-screen.tsx` 1 处 emoji
> + `icons.tsx` 1 个新图标；**不改任何交互逻辑、事件绑定与数据流**（协议总则 5）。

---

## 3.1 今天（comfort-today：简报弹窗 + 事件卡流 + 镜像行 + 速聊）

自上而下：

```
┌──────────────────────────────┐
│ HUD（不变：日chip·活命钱·债）  │  ← 基准 A-1（#B85A0A 全局生效）
│ ┌─「开始今天」弹窗─────────┐ │
│ │ 第 N 天  · 常用手机        │ │  ← 字阶不变
│ │ ┌────────┬────────┐      │ │
│ │ │ 活命钱  │ 欠的债  │      │ │  ← 余额格 gap 8→保持，格内 8px 6px→8px 8px   [C]
│ │ └────────┴────────┘      │ │
│ │ ▤ 今天的规矩（虚线框）     │ │  ← 行距 10→8px，框内边距 12px            [C]
│ │ ▍经文块（左边线金）        │ │  ← 不变                                  [—]
│ │ ⚡ 突发事件行（橙 7% 底）   │ │  ← 彩条色语义化，见 3.1-b                 [A-2]
│ │      [ 开始今天 ] 52px     │ │  ← 主 CTA 底色随 A-1 自动达标             [A-0]
│ └──────────────────────────┘ │
│ ┌─ 事件卡（每张）───────────┐ │
│ │▎头像  名字 · 红包           │ │  ← 左彩条按语义四分                       [A-2]
│ │  ▶ 语音 12″                │ │  ← 秒数 12px n6 不透明                    [B]
│ │  [收下 +5元] [不要（…）]    │ │  ← 金额 tabular-nums                     [B]
│ └──────────────────────────┘ │
│ ┌─ 两台手机（虚线框楷体）────┐ │  ← 不变（已是叙事插页的正确形态）          [D]
│ └──────────────────────────┘ │
│ 家人                          │
│ ┌─妈──────[ 回她 ]─────────┐ │  ← label 12px；禁用态虚线灰不变            [E]
│ ┌─阿凯────[ 听他说 ]───────┐ │
│ …                            │
└──────────────────────────────┘
```

| # | 现状问题 | 调整 | 基准 |
|---|---|---|---|
| a | 弹窗内间距 8px 6px / 10px 裸值混用，分组无呼吸 | 卡内边距统一 12/16，组间距 20（余额格→规矩→经文→事件→CTA） | C |
| b | 事件卡彩条只有 `bf_demand` 有专属色，「信/钱/冲突」同橙 | 彩条四分：收钱=gold、要钱=danger、吵架=rose、剧情=story 靛蓝 + 剧情卡加 1px story 30% 描边 | A-2, D |
| c | 简报 label 11px n5（on n2 = 4.0 ❌） | 12px n6 | B |
| d | 事件卡金额/钱包数字随文排版 | `font-variant-numeric: tabular-nums` 一条规则覆盖金额类 | B |

## 3.2 通讯录（contacts）

```
│ 关系条（家庭 88 ▮▮▮▮▮░ 感情 41 ▮▮▮░░）│ ← label 12px n6；meter 不变   [B]
│ ┌─妈──…────────[ 回她 ]─┐  │
│ ┌─阿凯─…────────[ 听他说 ]┐ │
│ ┌─爸（置灰·点开纪念）────┐  │ ← 虚线分隔 + 楷体纪念文，不变              [D]
│ └─相亲花名册（阿姨推的）──┘  │ ← 认识的候选人入列，样式沿用速聊行        [E]
```

| # | 现状问题 | 调整 | 基准 |
|---|---|---|---|
| a | rel-strip label 11px + n5 在透明底（夜罩下 2.99 ❌） | 12px n6；夜间升档由 `.comfort-night` 作用域规则统一承接 | A-3, B |
| b | 相亲对象行与家人行无视觉差 | 候选人行加 `bd:` 前缀的次级样式（仅 tag「凤霞姨介绍的」胶囊），其余同速聊行 | E |

## 3.3 朋友圈（moments）

现状已较完整（卡片/点赞/评论/红点）。只做两处：

| # | 现状问题 | 调整 | 基准 |
|---|---|---|---|
| a | 评论作者名与正文同字重，扫读时层级平 | `mc-author` 加 600 字重 + accent-strong 色（on n0 = 5.96 ✅） | E |
| b | 发圈入口按钮沿用全局主色 | 不改（发圈是低频操作，不该抢主 CTA）——维持现状 | 克制原则 |

## 3.4 聊天记录（history）

| # | 现状问题 | 调整 | 基准 |
|---|---|---|---|
| a | 归档行「第 N 天 · 名字 · N 条」三段信息字重无层级 | 天数 n5→n6 12px，名字保持 600，条数弱化 n5（≥14px 场合允许） | B |
| b | 展开的 transcript 复用语音气泡 | 不改（与新基准令牌自动一致） | — |

## 3.5 钱包（wallet）

```
│ ┌──────────┬──────────┐ │
│ │ 活命钱 523 │ 妈给过 300 │ │ ← summary 四格：数字 tabular-nums；     [B]
│ │ 阿凯给过 52 │ 阿凯拿走 0 │ │    pos/neg 语义色不变                    [—]
│ └──────────┴──────────┘ │
│ ┌ 两本账（虚线框楷体）────┐ │ ← 不变                                    [D]
```

| # | 现状问题 | 调整 | 基准 |
|---|---|---|---|
| a | summary 金额字随文 | tabular-nums + 数字与标签间距 4px 刻度 | B, C |
| b | 「阿凯拿走」neg 色在暖纸上偏闷 | 不改色（danger on n0 = 5.87 ✅ 已达标）——只做间距 | 克制原则 |

## 3.6 聊天视图（comfort-chat）

| # | 现状问题 | 调整 | 基准 |
|---|---|---|---|
| a | 语音秒数 opacity .7 + 11px（≈3.3 ❌） | 12px n6、去 opacity | B |
| b | 气泡色散落硬编码 3 处 | 收敛 `--c-bubble/--c-bubble-ink` 令牌（值不变） | A-1 |
| c | 底部回复选项区 | 不改（交互冻结；样式随按钮四态自动统一） | E |

## 3.7 夜间态（comfort-night，全局）

| # | 现状问题 | 调整 | 基准 |
|---|---|---|---|
| a | 只染 day-chip/phone-tag，且合成对比 2.07/1.17 ❌ | chip 改实底 `--c-night-bg #2E3350`，文字 #F2ECDD，tag #C9CFEA | A-3 |
| b | 透明底元素（rel-strip/禁用提示）夜里 n5 = 2.99 ❌ | 一条 `.comfort-night` 规则：n5 级功能文字升 n6 | A-3 |
| c | 页面靛蓝罩保留 | 不改（已正确置于内容后，卡片不透明） | — |
| d | 气泡 | 不改——「家里的灯」夜里不变冷，这是设计立场 | A-3 |

## 3.8 工作手机上的入口（冷色主题侧，最小触碰）

| # | 现状问题 | 调整 | 基准 |
|---|---|---|---|
| a | `comfort-entry-btn` 渐变用了 `#E8853B`（与暖主题主色是两个橙） | 渐变端色改 `#C2620E`（暖主题现值，作为「另一部手机」的品牌色桥）；#F5D08C 端与气泡绿同源收敛 | A-1 |

## 3.9 图标与杂项

- `📨` → `icons.tsx` 新增 `letter`（信封，1.6 描边同源口径），剧情卡 sys 标记改用 `<Ico name="letter" size={30}/>`——修复红线违规（诊断 #1）。基准 E-1。
- 360px：语音条 min-width 92→84，气泡 max-width 78%→74%（仅此一个媒体查询）。基准 F-3。

---

## 令牌账目

| 动作 | 明细 |
|---|---|
| 新增 | `--c-story` `--c-bubble` `--c-bubble-ink` `--c-night-bg` `--c-night-text` `--c-night-tag` `--c-overlay` `--c-dawn`（8 个） |
| 复用 | `--c-accent`(值改 #B85A0A) `--c-accent-strong` `--c-gold` `--c-rose` `--c-success` `--c-danger` `--c-fs-*` `--c-s-*` `--c-r-*` `--c-shadow-*`（全部现值或一处值改） |
| 废弃 | `#AAB2D6` `rgba(60,55,88,.42)` 半透明 chip、散落 `#F5D08C/#5A3E10` 裸值 ×3、`rgba(58,42,20,.44)` 裸值、`#E8853B` |
| 不动 | 冷色主题全部、组件 DOM 结构、事件绑定、数据流、动画（voice-wave/phone-flip/reduced-motion 已达标） |

⛔ 产出完毕，停在协议规定的确认点：确认后进入第 4 阶段实施
（第一步落令牌 → 第二步全局组件 → 第三步逐模块，每步一次 commit，每步完成后停下汇报）。
