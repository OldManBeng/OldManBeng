# 03 · 实施记录 · 舒适圈视觉优化

> 协议：《视觉优化协议.md》第 4 阶段。三步走，每步一次 commit，实施前有 WIP 快照。

## 实施前快照
- `784df60` chore: 视觉优化前的 WIP 快照（工作区原有未提交改动 + site-audit 文档）

## 第一步：落地令牌（`763da6c`）
- `--c-accent` #C2620E → **#B85A0A**（唯一观感变化：白字主 CTA 4.16→4.66 AA 达标）
- 新增 8 个令牌：`--c-story` `--c-bubble` `--c-bubble-ink` `--c-night-bg`
  `--c-night-text` `--c-night-tag` `--c-overlay` `--c-dawn`
- `--c-fs-micro` 11px → 12px（功能标签下限）
- 头部注释的失实对比度声明改为实测值并注明脚本出处
- 气泡绿别名 `--bub-green/--bub-green-ink`、页顶晨光、弹窗遮罩改引令牌

## 第二步：全局组件切令牌（`b77d4d2`）
- **夜态覆盖**（基准 A-3）：day-chip / phone-tag 改实底夜色；`.comfort-night .btn:disabled` 升 n6；注释写明「气泡不变冷」的设计立场
- **事件卡彩条语义四分**（基准 A-2）：mom_gift/bf_packet=金、bf_demand=红、quarrel=玫红、story=靛蓝 + 30% 描边
- **红线修复**：`📨` → `icons.tsx` 新增 `letter` 信封图标（1.6 描边同源），`.cec-sys-mark` 改靛蓝底 SVG 徽标
- 语音秒数：12px n6 去 opacity（≈3.3→8.26）
- 功能标签色 n5→n6（day-money span / day-row-label / day-rule-label / day-sub）
- 金额 `tabular-nums`（事件卡按钮 / 钱包 summary / 关系条）
- 切机入口渐变端色 `#E8853B` → `#C2620E`（冷色主题侧，两台手机的橙统一）
- 360px：voice-bar 92→84、cbubble 78%→74%

## 第三步：逐模块布局调整（`7d572f0`）
- 「开始今天」弹窗：卡内边距 / 余额格 / 规矩组（组间 20px）/ 头部全部收敛 4px 刻度
- 事件卡 / 经文块：12×16 统一卡内边距；突发事件行负 margin 随动
-  verse-gloss 6→4px

## 未做（克制原则 / 超范围记录）
- 朋友圈评论作者名（`mc-author`）与聊天记录行层级：勘探后发现现状已达标
  （600 字重 + accent 色 / 12px muted），**未改动**——无问题不制造问题
- 相亲候选人行次级胶囊：现有 tag 文案已含「凤霞姨介绍的」语义，未加视觉差
  （如需可入下一轮）
- 交互逻辑、DOM 结构、数据流、动画：零改动（协议总则 5 达成）

## 已知豁免（机械扫描残留的裸值）
`voice-wave` 声波条高度（6/11/15px 图形几何）、播放三角 border（6px）、
气泡入场 translateY(10px)（动画位移）、切机 chip padding 2×10（胶囊微调）、
遮罩 padding 18px——均为图标/动画几何，非布局间距，豁免。
