# v4.13.1 回归验证（第 6 阶段修复后）

> 对应总报告 `05-总报告.md`。修复于 2026-09-11 完成，本文件按协议 §6.4 记录修复与回归。

## 修复清单

### P0 · H1 存折金额矛盾 ✅
- `endings.ts:82`「五位数」→「七千二」——与 `zhou-script.ts:164` / `main-packs.ts:333` 的 7200 锚对齐。1 词改动。

### P1 · M1 王总儿子数学成绩统一 ✅
- 锚定剧情链的 41（`wang-script.ts:100`）。
- `life-events.ts:124`：数学 28 → 「第二次月考，数学还是 41，没提」（显式标注另一次考试，保留人生线语气）。
- `wang-script.ts:396` 问候池「儿子月考及格了」→「儿子月考数学又 41 他不许我说 可我还是想跟人说」（与 41 一致，保留王总空格断句话风）。

### P1 · M2 王总「丫头」越界 ✅（分类处理）
- 真穿帮 1 处已修：`main-packs.ts:138`「你小子。哦不，你丫头。」→「领导，这话我只敢跟你说。」
- 保留 3 处（`wang-script.ts:255/414/418`）：均在 sweet_daughter 学妹人设专属通道（personaText/greet_sd 池）——`dialogue-audit.test.ts` 的 `greet_*` 豁免已覆盖，属有意的人设差异化设计（学妹是全游戏里最像女儿的角色，王总对学妹偶尔软成父亲腔是人物层次）。
- `main-packs.ts:284/304` 复核为 lao_li 的 incoming（老李专属「丫头」），合规不动。

### P2 · H2 阿豪/陈工坦白线结局差异化 ✅（内容增补）
- 新增 4 结局（`endings.ts`，requires 对接已有 flag）：
  - `end_hao_confessed`「键盘、猫，和四十一次晚安」（hao_confessed）
  - `end_hao_lied`「设备处理，含一台4090」（hao_lied_final）
  - `end_chen_confessed`「第 5 条」（chen_confessed）
  - `end_chen_gave`「买图纸」（chen_gave_savings）
- 基调对齐：阿豪线从「猫的一生换算」长出、陈工线从「编号清单/公差 ±0.02/买图纸」长出——全部引用已有话术锚，不引入新设定。
- 配套：`gathas.ts` 4 条偈（gathas.test 红线「每结局必有偈」倒逼）；`state-machine.ts scoreEnding` 在老李坦白线同级接入。
- 优先级位置：存折/车库/穿帮 > 账本 > **坦白线（老李+阿豪+陈工）** > 差一点。

### 额外 · 红包话术与实际行为一致性（用户报告，本批同修）
- 新机制 `ChainOption.autoPacket?: number`（`types/script.ts`）：非开口选项叙述「他转了红包」的必须声明金额。
- 引擎（`state-machine.ts` pick_option normal-reply 分支）：autoPacket → 红包横幅 + 来源字幕 + 流水 + totalReceived/timesPaid/daysSincePaid/stats 落账（与教学红包/ask 红包同通路）。他主动的：wariness 只吃 ask 成功的一半，daysSincePaid 归零。
- 数据接线 5 处：`packs-li.ts:100`（老李 3 元）、`incoming-replies.ts` 阿豪钱包 2 处（50/60）+ 保安补助 2 处（30/200）+ 老李生日礼物 200。
- 反向（说之前转过但没发生）：`incoming-replies.ts:131` 阿豪「这个月第二次了」改为不依赖历史的表述；引擎两处 timesPaid 门控（v4.3.3 已有）保持。
- 复核免改：chen `ask_success/packet_received` 池（由红包通路渲染，横幅先出）；`direct-ask.ts`（引擎完整落账）；`incidents.ts:352` 老李 500 事件（事件自带 money 效果落账）。
- 常驻回归：`v4131-packet-consistency.test.ts` 3 条红线（落账齐/无幽灵入账/timesPaid 门控成立）。

## 回归验证

- `npx tsc -b`：零错。
- `npx vitest run`：**228/228 全绿**（221 旧 + v4.13 人设头像 4 + v4.13.1 红包一致性 3）。含：
  - `dialogue-audit.test.ts` 全量话术审计（949 套/2738 开场白/2191 选项）——M2 改动后无越界。
  - `simulation.test.ts` 30 种子自动跑局——scoreEnding 接新结局后仍全部落到设计内结局。
  - `gathas.test.ts`——每结局有偈红线。
  - `v412-wave2/v412-freshness/v433-features`——教学红包/钱包冷却/「已经给过了」门控回归未破。
- `npm run build`：通过。

## 残留（已确认不修）

- `wang_sea_promise`/`wang_backed_off` 等「关系状态调味」flag 只写不读——非硬伏笔，有意留白。
- 老 li 支线 c_li_wise 同行死讯 vs 人生线 day24 同一事件（审查特别核查项）：属同一事件跨通道复现（老李逢人就说），与「94 秒红灯」同模式，保留。
