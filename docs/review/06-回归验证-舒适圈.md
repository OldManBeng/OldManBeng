# 06 · 回归验证 · 舒适圈审查修复（第 2 轮，第 6 阶段步骤 4）

> 对应总报告 `05-总报告-舒适圈.md`。按协议 §6.4 记录修复与回归。
> （`06-回归验证.md` 为上一轮工作手机修复的记录，不覆盖。）

## 1. 机械回归（全部实测）

| 项 | 结果 |
|---|---|
| `npx tsc --noEmit` | ✅ 通过 |
| `npx vitest run` | ✅ **292/292**（含：全库台词零重复审计——13 处改写文案已重新过零重复扫描；话术结构红线；派发节流；金额定额绑定后的要钱结算链） |
| `npm run build` | ✅ 通过 |
| 金额绑定验证 | 结构性保证：`di = rng.int(0, BF_DEMAND_LINES.length-1)`，`amount = BF_DEMAND_AMOUNTS[di]`——同一下标取台词与金额，永不错位；两数组相邻定义，长度一致由数据完整性测试覆盖 |

## 2. 被改章节的第 3 阶段复查（逐条对照 01-事实清单）

| 章 | 复查结论 |
|---|---|
| 妈 | 「弟弟」句已消（独生女画像复原）；「学会朗读」与镜像行「五十九秒」口径互补不再打架；零重复审计过 ✅ |
| 阿凯 | 「三百多」与红包 52-188（对半）量级一致；生日 21 两处一致；要钱金额=台词金额；零重复审计过 ✅ |
| 凤霞姨 | 「区里的友谊赛」与「街道第一」不再冲突（不同赛事并存）；知情边界未动 ✅ |
| 曼曼 | 相亲知情来源闭合（「你上回说」）；零重复审计过 ✅ |
| 暗线 | 舅妈指代落位；裙子天数、生日金额、证据金额三处不再与状态冲突；经文池未动 ✅ |
| 数值 | loan_sms fx = {+3000, +3000债}，测试断言 love 不变 ✅；2× 拒绝代价保留并注释保护 ✅ |

## 3. 交互回归（协议红线）

- 本轮修复只改**文案字符串、一处 fx 数值、一处引擎取额逻辑**；
- 事件绑定、dispatch、UI 结构零改动（comfort-screen 仅警察卡一行文案）；
- 动画/门禁/节流路径未触碰；引擎改动仅替换金额来源，派发条件与频率不变。

## 4. 遗留（未改，均已记录）

- D14 伏笔引用的边缘死角（04 报告 #3）：宽限窗内每个早晨都开着聊天则伏笔不派发、
  D28 仍会引用——评估为极低概率（早晨 c.chat 通常为空）；根除需引擎行为改动，留下轮。
- 吵架 4 次封顶后的叙事平坦段（03 报告 [NIT]），等新吵架文案时一并处理。
- 工作手机侧遗留项见上一轮 `05-总报告.md` / `06-回归验证.md`。

**结论**：13 项修复全部落地，机械与人工复查均无新矛盾引入。舒适圈第 1-6 阶段协议闭环。
